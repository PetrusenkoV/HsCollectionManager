@model HsCollectionManager.Models.UserModel

@{
    ViewBag.Title = "SignUp";
}

<h2>SignUp</h2>
@using (Html.BeginForm())
{
    @Html.TextBoxFor(x => x.Name);
    <input type="submit" value="Sign Up!"/>
}
